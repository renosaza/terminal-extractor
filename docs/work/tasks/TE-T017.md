---
id: TE-T017
type: task
status: draft
task_status: doing
scope: tmux-session-owner
parent: TE-E05
depends_on: [TE-T004, TE-T008]
owner: Codex
priority: P0
---
# Dedicated tmux backend и lifecycle

## Результат

Создавать/регистрировать pane в отдельном private socket namespace с минимальной собственной config и долгоживущим shell owner.

## Приёмка

User tmux server/config не затронуты. На each command новый shell не создаётся. Capture gate существует до workload. Registry связывает pane/socket/generation; закрытие одного session не вызывает kill-server. Existing arbitrary pane требует локальной регистрации.

## Проверка

PARTIAL feasibility, 2026-09-24: `sh probes/tmux/probe.sh` PASS на отдельном private socket и `/dev/null` config: два fixture sessions, capture gate до test workload, закрытие первой по точному `=session-name` сохраняет pane ID/PID второй. Голое имя `te-probe` в `has-session -t` ошибочно нашло `te-probe-other` по префиксу; для lifecycle нужен exact target. Продуктовый owner/registry, gateway disconnect, pane exit/recreate, namespace collision, отсутствующий tmux и реальная GUI pane — NOT_RUN. Не считать probe реализацией TE-T017. AT-02/03/12, TE-C02/03/06/07.

PARTIAL implementation, 2026-09-24, checked code revision `f232de0`: `ManagedTmux` создаёт только detached служебную pane с `/bin/sleep` в отдельном socket namespace и `/dev/null` config. Binding содержит явные session ID/generation, tmux session/pane ID и socket path; revalidate проверяет owner/mode/inode namespace и точный pane, close адресует сохранённый session ID и не делает kill-server. `swift run TermexManagedTmuxCheck` PASS: два fixture bindings, stale generation, missing tmux, socket collision и закрытие одной без смены PID второй. `swift run TermexRegistryCheck`, `swift run TermexConfigCheck` и `git diff --check` PASS. Для этого product owner capture gate, долгоживущий shell, GUI attach/consent, gateway disconnect, pane exit/recreate и таймаут с неоднозначным server-side результатом — NOT_RUN. При таком таймауте есть best-effort cleanup по точному имени и найденному session ID, но отсутствие осиротевшей pane не доказано; private namespace следует сохранить для диагностики. Это не пользовательская сессия и не заменяет native чтение истории existing Ghostty pane.

PARTIAL live fixture, 2026-09-24, revision `e6f8fe7`: после явного выбора пользователем открытой тестовой Ghostty pane `visible_attach_probe.sh` временно подключил её к private tmux session с shell. Recorder был готов до respawn shell; первая живая попытка дала видимый маркер, но `capture_pipe_marker=NOT_OBSERVED`, причина не установлена. После добавления раздельной проверки screen/pipe и ожидания пользователь повторил опыт: скрипт сообщил `gui_screen_marker=PASS`, `capture_pipe_marker=PASS`, затем исходный prompt вернулся после `tmux detach-client`. `sh -n probes/tmux/visible_attach_probe.sh`, `sh probes/tmux/visible_attach_probe.sh --self-check` и `sh probes/tmux/probe.sh` PASS. Скрипт сохранял лишь пустые marker files в private temp, не выводил terminal content. Это ручной fixture, не product capture gate/registry/view binding; долгоживущий owner и управление lifecycle через host остаются открытыми. История уже существующей Ghostty pane в tmux не переносится.

## Связи

[Эпик](../epics/TE-E05.md), [архитектура](../../architecture/README.md).
